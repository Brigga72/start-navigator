# Cruise Navigator v0.9.5

- Fixes false update prompts when the running build is already current or newer.
- Uses semantic version comparison instead of simple string inequality.
- Uses a fresh service-worker cache namespace.
- Keeps the existing ship, Explore, CocoCay, Hideaway Beach, and navigation features.
